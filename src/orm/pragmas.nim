template pk* {.pragma.}
  ## Mark field as primary key

template immutable* {.pragma.}
  ## Mark field immutable

template unique* {.pragma.}
  ## Mark field as unique

template fk*(val: typed) {.pragma.}
  ## Mark
